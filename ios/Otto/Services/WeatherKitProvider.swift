import Foundation

/// TODO(Phase 5): WeatherKit — requires the paid Apple Developer account.
///
/// When the account is active, implement on-device weather here (WeatherKit
/// framework, no API key, no server round trip) producing the shared
/// CurrentWeather/HourlyForecast types, and delete
/// server/src/services/weather/ — the brief request will then carry weather
/// computed on-device. Until then the server's Open-Meteo provider covers
/// everything, and this file exists so the seam is visible.
enum WeatherKitProvider {}
