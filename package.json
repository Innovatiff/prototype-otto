{
  "name": "otto",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Otto — a voice-first personal assistant (workspace root)",
  "workspaces": [
    "shared",
    "server"
  ],
  "scripts": {
    "codegen": "tsx shared/codegen/swift.ts",
    "build": "npm run build -w @otto/shared && npm run build -w @otto/server",
    "typecheck": "npm run build -w @otto/shared && npm run typecheck -w @otto/shared && npm run typecheck -w @otto/server",
    "test": "npm run build -w @otto/shared && npm run test -w @otto/shared && npm run test -w @otto/server",
    "dev": "npm run build -w @otto/shared && npm run dev -w @otto/server",
    "deploy": "bash server/deploy.sh"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
